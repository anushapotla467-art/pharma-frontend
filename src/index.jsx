import React from "react";
import ReactDOM from "react-dom/client";
import UpdateBatchStatus from "./UpdateBatchStatus"; // adjust path if needed

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <UpdateBatchStatus />
  </React.StrictMode>
);
